# JavaScript Web Scraping Project

This project consists of several JavaScript scripts for web scraping and working with APIs. Each script performs a specific task related to web scraping and API requests.

## Prerequisites

- Node.js (v14.x)
- npm (Node Package Manager)
- Required npm modules:
  - `request`

You can install the required npm modules using the following commands:

```bash
$ sudo npm install request --global
