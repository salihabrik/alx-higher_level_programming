#!/usr/bin/python3

def add_tuple(tuple_a=(), tuple_b=()):
    first-element = tuple_a + (0, 0)
    second-element = tuple_b + (0, 0)


    results = (first-element[0] + second-element[0],first-element[1] + second-element[1])
    

    return (results)
